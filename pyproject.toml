[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "url_shortener"
version = "0.1.0"
dependencies = [
    "pyshorteners"
]

[project.scripts]
url-shortener = "url_shortener.cli:main"